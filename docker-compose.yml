services:
  client:
    build: ./client
    environment:
      - API_URL=http://server:5001
    ports:
      - "8080:80"
  server:
    build: ./server
    env_file:
      - ./server/.env
    ports:
      - "5001:5001"